define(function(require) {
  'use strict';

  var inherits = require('../../../../lib/util/inherits');

  describe('inherits', function() {
    it('exists', function() {
      assert.ok(inherits);
    });
  });
});
